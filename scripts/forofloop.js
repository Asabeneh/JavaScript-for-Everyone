const numbers = [1, 2, 3, 4, 5];
for (const number of numbers) {
  console.log(number);
}

const names = ['Asabeneh', 'Brook', 'David', 'Mohammed', 'Marta'];

for (const name of names) {
  console.log(name);
}

const countries = ['Finland', 'Sweden', 'Denmark', 'Norway'];
for (const country of countries) {
  console.log(country);
}
